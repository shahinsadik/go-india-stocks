// /** @type {import('next').NextConfig} */
// const nextConfig = {
    
// };

// export default nextConfig;
const nextConfig = {
    images: {
      domains: ["media.istockphoto.com"],
    },
  };
  
  export default nextConfig;
